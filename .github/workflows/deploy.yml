name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # -- Push images to registry ------------------------------------------
      # Uncomment and configure for your registry (ECR, GCR, Docker Hub, etc.)
      #
      # - name: Log in to registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ${{ secrets.REGISTRY }}
      #     username: ${{ secrets.REGISTRY_USER }}
      #     password: ${{ secrets.REGISTRY_PASSWORD }}
      #
      # - name: Build & push dariyah-core
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ./services/dariyah-core
      #     push: true
      #     tags: ${{ secrets.REGISTRY }}/dariyah-core:${{ github.sha }},${{ secrets.REGISTRY }}/dariyah-core:latest
      #
      # - name: Build & push frontend
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ./apps/frontend
      #     push: true
      #     tags: ${{ secrets.REGISTRY }}/dmf-frontend:${{ github.sha }},${{ secrets.REGISTRY }}/dmf-frontend:latest

      # -- Run DB migrations ------------------------------------------------
      # - name: Run migrations
      #   run: |
      #     docker run --rm \
      #       -e DATABASE_URL=${{ secrets.DATABASE_URL }} \
      #       ${{ secrets.REGISTRY }}/dariyah-core:${{ github.sha }} \
      #       alembic upgrade head

      # -- Restart services -------------------------------------------------
      # Configure for your deployment target (ECS, Cloud Run, Fly.io, etc.)
      - name: Deploy placeholder
        run: echo "Deploy step — configure for your cloud provider"
